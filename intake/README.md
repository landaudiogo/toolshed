# Input

This script parses the html file loaded by `datumprikker.nl` (check the network section while inspecting the webpage on reload). 

# Output

The output is a set of candidate timeslots for each student based on their availability
